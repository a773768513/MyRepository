测试：
覆盖历遍测试
1：1.语句覆盖每条语句至少执行一次。
2.判定覆盖每个判定的每个分支至少执行一次。
3.条件覆盖每个判定的每个条件应取到各种可能的值。
4.判定/条件覆盖同时满足判定覆盖条件覆盖。
5.条件组合覆盖每个判定中各条件的每一种组合至少出现一次。
6.路径覆盖使程序中每一条可能的路径至少执行一次。
历遍
1：Help(OK)
2:参数模式不对(OK)
3:文件打开失败,提示且退出(OK)
4：读取一行为NULL，且文件未到末尾，既读取失败提示退出(暂无)
5：读取一行为NULL，文件到达末尾(OK)；
6：行长度多于127，将数据转换为动态内存(OK)；
7：最后一行行长度为127.使用纯字符成功，使用汉字失效（汉字占两个字节，调整后正常）
8：行长度低于127，使用栈内存，（OK）


边界条件：1：行长度为0；（OK）
2 行长度为128，127，129，126，130;（使用malloc函数改变了新的内存）（解决，正常）
3：行长度为255； 256 ，254，253，257（使用realloc函数）（解决，正常）
4：最后一行为空行的情况（已测试，解决）
5：

错误条件示范：
1：打开文件非文字文件。打开电影文件，程序可以正常运行，但是没有找出任何结果；

随机测试，复制网站上的字符，用来查找特定字符（OK）


目前测试向量约一万行，正常运行。